<%= form_for(@auth_group) do |f| %>
  <% if @auth_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@auth_group.errors.count, "error") %> prohibited this auth_group from being saved:</h2>

      <ul>
      <% @auth_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
