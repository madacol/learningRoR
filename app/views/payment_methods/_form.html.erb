<%= form_for(@payment_method, namespace: namespace_id, :html=> {:id => namespace_id}) do |f| %>
  <% if @payment_method.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@payment_method.errors.count, "error") %> prohibited this payment_method from being saved:</h2>

      <ul>
      <% @payment_method.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group row">
    <div class="col-sm-2">
      <%= f.label :account_id, "Cuenta", class: "control-label" %><br>
    </div>
    <div class="col-sm-8">
      <%= f.collection_select( :account_id, Account.all, :id, :record_name_to_show, { prompt: '-Seleccionar-'}, { class: "form-control c-select chosen-select" }) %>
    </div>
  </div>
  <div class="form-group row">
    <div class="col-sm-2">
      <%= f.label :method, "Método", class: "control-label" %><br>
    </div>
    <div class="col-sm-10">
      <%= f.text_field :method, class: "form-control", :required => true %>
    </div>
  </div>
  <div class="form-group row">
    <div class="col-sm-2">
      <%= f.label :status, "Estado", class: "control-label" %><br>
    </div>
    <div class="col-sm-10">
      <%= f.collection_select( :status, PaymentMethod.statuses, :first, :first, { prompt: '-Seleccionar-'}, { class: "form-control c-select chosen-select" }) %>
    </div>
  </div>
<% end %>