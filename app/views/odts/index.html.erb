<p id="notice"><%= notice %></p>

<h1>Listing <%= Odt.new.table_name_to_show %></h1>

<p><%= render 'form', odt: @odt, btn_name: 'Nuevo Registro', id: 'new_odt' %> </p>


<div class="table-responsive">
  <table  class="table table-sm table-hover table-striped table-bordered">
    <thead>
       <tr>
        <th>Code</th>
        <th>Alias</th>
        <th>Description</th>
        <th><%= RazonSocial.new.table_name_to_show %></th>
        <th>Monto contrato</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <tr>
      <% @odts.reverse.each do |odt| %>
         <td><%= odt.code %></td>
         <td><%= odt.alias %></td>
         <td><%= odt.description %></td>
         <td><%= link_to odt.razon_social.record_name_to_show, odt.razon_social%></td>
         <td><%= odt.monto_contrato %></td>
         <td><%= render 'form', odt: odt, btn_name: 'Editar', id: "edit_odt_#{odt.id}" %></td>
         <td><%= link_to '×', odt, method: :delete, data: { confirm: 'Are you sure?' }, class: "close equis" %></td>
      </tr>
      <% end %>
    </tbody>
  </table>
</div>