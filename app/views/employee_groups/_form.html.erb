<%= form_for(@employee_group) do |f| %>
  <% if @employee_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@employee_group.errors.count, "error") %> prohibited this employee_group from being saved:</h2>

      <ul>
      <% @employee_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= label_tag( 'employee_group_employee_id', Employee.new.table_name_to_show ) %><br>
    <%= f.select( :employee_id, options_for_select( Employee.all.collect{ |e| [e.record_name_to_show, e.id ] }, @employee_group.employee_id ), :prompt => '--Seleccionar--' ) %>
  </div>
  <div class="field">
    <%= label_tag( 'employee_group_auth_group_id', AuthGroup.new.table_name_to_show ) %><br>
    <%= f.select( :auth_group_id, options_for_select( AuthGroup.all.collect{ |e| [e.record_name_to_show, e.id ] }, @employee_group.auth_group_id ), :prompt => '--Seleccionar--' ) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
