<%= form_for(@blah) do |f| %>
  <% if @blah.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@blah.errors.count, "error") %> prohibited this blah from being saved:</h2>

      <ul>
      <% @blah.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
