<p id="notice"><%= notice %></p>

<h1>Listing Blahs</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @blahs.each do |blah| %>
      <tr>
        <td><%= blah.name %></td>
        <td><%= link_to 'Show', blah %></td>
        <td><%= link_to 'Edit', edit_blah_path(blah) %></td>
        <td><%= link_to 'Destroy', blah, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Blah', new_blah_path %>
