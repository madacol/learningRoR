<%= form_for(@inversion) do |f| %>
  <% if @inversion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@inversion.errors.count, "error") %> prohibited this inversion from being saved:</h2>

      <ul>
      <% @inversion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :code %><br>
    <%= f.text_field :code %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
